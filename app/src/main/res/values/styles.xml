<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FormTitleTheme">
        <item name="android:paddingTop">@dimen/paddingDefault</item>
        <item name="android:paddingBottom">@dimen/paddingDefault</item>
        <item name="android:paddingStart">@dimen/paddingDefault</item>
        <item name="android:paddingEnd">@dimen/paddingDefault</item>
        <item name="android:textSize">@dimen/textSizeExtraLarge</item>
    </style>

    <style name="FormFieldTheme">
        <item name="android:paddingTop">@dimen/paddingDefault</item>
        <item name="android:paddingBottom">@dimen/paddingDefault</item>
        <item name="android:paddingStart">@dimen/paddingDefault</item>
        <item name="android:paddingEnd">@dimen/paddingDefault</item>
        <item name="android:textSize">@dimen/textSizeDefault</item>
    </style>

</resources>
